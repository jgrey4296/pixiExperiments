define(['underscore','phaser'],function(_,Phaser){

    var RoomState = function(game,roomDescription){
        //save the room description
    };

    RoomState.prototype.preload = function(){

    };

    RoomState.prototype.create = function(){
        //create the individual items and actors that will be in the room?
    };

    RoomState.prototype.update = function(){

    };

    RoomState.prototype.shutdown = function(){

    };

    return RoomState;
});
